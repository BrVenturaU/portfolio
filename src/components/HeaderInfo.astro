---
const { icon, title, description } = Astro.props;
---

<div class="flex items-center gap-4 my-2 first-of-type:mt-0 last-of-type:mb-0">
  <span class={`text-2xl ${icon}`}></span>
  <div>
    <p class="font-bold">{title}</p>
    <p>{description}</p>
  </div>
</div>
